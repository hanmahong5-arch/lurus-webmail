apiVersion: v1
kind: Secret
metadata:
  name: stalwart-secrets
  namespace: mail
  labels:
    app: stalwart
type: Opaque
stringData:
  STALWART_ADMIN_PASSWORD: "Lurus@ops"
  # TLS certificates managed by cert-manager (see certificate.yaml)
  # If using manual certs, mount them via volume
