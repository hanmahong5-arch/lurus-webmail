name: Bug report
description: File a bug report for Lurus Mail
labels: [bug]

body:
  - type: textarea
    id: description
    attributes:
      label: What happened?
      description: Describe the issue.
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: Environment
      placeholder: |
        - Lurus Mail version:
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ### Logs (required)
        From the directory containing your `docker-compose.yml`, run:

        ```bash
        docker compose logs --timestamps --no-color > webmail-logs.txt
        ```

        Then attach **webmail-logs.txt** to this issue.

  - type: checkboxes
    id: logs_confirm
    attributes:
      label: Confirm
      options:
        - label: I have attached `webmail-logs.txt`
          required: true
